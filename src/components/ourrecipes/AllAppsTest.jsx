import React from 'react';
import TestApp from './TestApp';

const AllAppsTest = () => {
    return (
        <div>
            {
                // AllRecipes.map(Recipe => <TestApp key={Recipe.id} Recipe={Recipe}></TestApp>)
            }

        </div>
    );
};

export default AllAppsTest;