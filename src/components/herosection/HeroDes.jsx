import React from 'react';

const HeroDes = ({Description}) => {
    return (
        <div>
            <p className='text-center text-white text-lg mb-10'>{Description}</p>
        </div>
    );
};

export default HeroDes;